en:
  graphql:
    inputs:
      user:
        sign_up_input:
          desc: Sign up user via email
          args:
            first_name: User's first name
            last_name: User's last name
            email: User's account email
            password: User's account password
            password_confirmation: User's account password confirmation
        sign_in_input:
          desc: Sign in user via email
          args:
            email: User's account email
            password: User's account password
        item_id:
          desc: Create or destroy item via id
          args:
            id: id for destroy/creation
        create_list:
          desc: Create new users list
          args:
            name: Name of the new list
        list_item:
          desc: Add/Remove movie in user's list
          args:
            list_id: User's list ID
            movie_id: Movie ID
